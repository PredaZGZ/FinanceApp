{
  "name": "tauri-desktop",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "env -u WAYLAND_DISPLAY DISPLAY=:0 XDG_SESSION_TYPE=x11 GDK_BACKEND=x11 WEBKIT_DISABLE_DMABUF_RENDERER=1 sh -c \"pnpm --filter web dev -- --host localhost & wait-on http://localhost:5173 && pnpm exec tauri dev\"",
    "dev:soft": "env -u WAYLAND_DISPLAY DISPLAY=:0 XDG_SESSION_TYPE=x11 GDK_BACKEND=x11 WEBKIT_DISABLE_DMABUF_RENDERER=1 LIBGL_ALWAYS_SOFTWARE=1 pnpm exec tauri dev",
    "build": "pnpm exec tauri build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.22.0",
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.4"
  }
}