{
  "name": "FinanceApp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter api dev",
    "dev:desktop": "pnpm --filter tauri-desktop dev",
    "dev": "pnpm -r --parallel --filter api --filter tauri-desktop dev",
    "build:web": "pnpm --filter web build",
    "build:api": "pnpm --filter api build",
    "build:desktop": "pnpm --filter tauri-desktop build",
    "build": "pnpm build:web && pnpm build:api && pnpm build:desktop"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.22.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "wait-on": "^9.0.3"
  }
}